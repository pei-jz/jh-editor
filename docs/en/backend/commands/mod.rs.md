# commands/mod.rs — Command Module Declarations

Module declarations for commands (9 lines).

**Path**: `src-tauri/src/commands/mod.rs`

Submodules: `fs`, `search`, `parser`, `app`, `pty`, `lsp`, `git`, `large_file`