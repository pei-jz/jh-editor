# Network.js — Network Utilities

HTTP request utilities.

**Path**: `src/modules/utils/Network.js` (877B)

| Function | Description |
|----------|-------------|
| `fetchWithTimeout(url, options, timeout)` | Fetch with timeout |

Uses Tauri HTTP plugin.