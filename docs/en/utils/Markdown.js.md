# Markdown.js — Markdown Rendering Configuration

Manages marked.js and mermaid.js rendering settings.

**Path**: `src/modules/utils/Markdown.js` (3.0KB)

| Function | Description |
|----------|-------------|
| `configureMarkdown()` | Custom marked.js renderer setup |
| `initMermaid()` | Initialize mermaid |

Extends code blocks, tables, and link handling via custom renderer.