# JsonParser.js — JSON Structure Parsing

Parses JSON into tree structure.

**Path**: `src/modules/utils/JsonParser.js` (4.4KB)

| Function | Description |
|----------|-------------|
| `parseJson(content)` | Convert JSON string to StructuredNode tree |

Recursively trees arrays, objects, and primitive values.