# XmlParser.js — XML Structure Parsing

Parses XML into tree structure.

**Path**: `src/modules/utils/XmlParser.js` (6.1KB)

| Function | Description |
|----------|-------------|
| `parseXml(content)` | Convert XML string to StructuredNode tree |

Generates complete XML tree including attributes and text nodes.