# HtmlParser.js — HTML Structure Parsing

Parses HTML into tree structure.

**Path**: `src/modules/utils/HtmlParser.js` (5.1KB)

| Function | Description |
|----------|-------------|
| `parseHtml(content)` | Convert HTML string to StructuredNode tree |

Generates structured data tree (`{id, key, value, type, children}`). Used by StructureView.