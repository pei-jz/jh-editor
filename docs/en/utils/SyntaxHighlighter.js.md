# SyntaxHighlighter.js — Unified Syntax Highlighting Entry

Integration entry point for Shiki/CMHighlighter.

**Path**: `src/modules/utils/SyntaxHighlighter.js` (715B)

| Function | Description |
|----------|-------------|
| `init()` | Initialize Shiki |
| `highlight(code, lang)` | Select optimal highlighter |

Branch: Shiki initialized → use Shiki, not initialized → fallback to CMHighlighter