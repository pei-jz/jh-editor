# Markdown.js — Markdownレンダリング設定

marked.jsとmermaid.jsのMarkdownレンダリング設定を管理します。

**パス**: `src/modules/utils/Markdown.js`（3.0KB）

| 関数 | 説明 |
|------|------|
| `configureMarkdown()` | marked.jsのカスタムレンダラー設定 |
| `initMermaid()` | mermaid初期化 |

marked.jsのカスタムレンダラーでコードブロック、テーブル、リンクの処理を拡張。