# JsonParser.js — JSON構造解析

JSONをツリー構造に解析します。

**パス**: `src/modules/utils/JsonParser.js`（4.4KB）

| 関数 | 説明 |
|------|------|
| `parseJson(content)` | JSON文字列をStructuredNodeツリーに変換 |

配列、オブジェクト、プリミティブ値を再帰的にツリー化。